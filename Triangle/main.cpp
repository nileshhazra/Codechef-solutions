#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> v;
    //----Accepting Values----
    for(int i=0;i<n;++i)
    {
        for(int j=0;j<=i;++j)
        {
            cin>>value;
            v.push_back(value);

        }
    }
    //----Calculating Sum-----
    int sum = 0;int max_num;
    for(int i=0;i<n;++i)
    {
        for(int j=i;j<=i;++j)
        {
            if(i==0)
                sum = v[i];
            else



        }
    }
    return 0;
}
