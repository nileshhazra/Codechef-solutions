#include <bits/stdc++.h>
using namespace std;
int main() {
	int t,i,d;
	long n;
	cin>>t;
	for(i=0;i<t;++i)
	{
	    cin>>n;
	    for(d=2;d<=sqrt(n) && n%d;++d)
	    {

	    }
    if(d>sqrt(n))
    cout<<"yes\n";
    else
    cout<<"no\n";

	}

	return 0;
}

